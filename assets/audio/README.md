# Runtime Audio

Place cooked runtime clips here using paths declared in
`assets/config/audio.toml`.

Keep raw sessions, untrimmed recordings and mix masters under
`assets/source/audio/`; runtime loads only resolved files from this directory.
See `docs/audio-system.md` for bus, cue, loudness, variation and adaptive-music
contracts.
