# Audio Sources

Store raw recordings, DAW sessions, untrimmed takes, stem masters and export
notes here. Runtime-ready clips are cooked into `assets/audio/` using logical
paths declared in `assets/config/audio.toml`.

Do not load files from this directory at runtime.
